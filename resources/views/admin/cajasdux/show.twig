{% extends "layouts/app.twig" %}
{% block title %}{{ lang.cajasdux }} {{ lang.details }}{% endblock %}
{% block content %}
    <div class="page-wrapper">
        <div class="page-content">
            <!--breadcrumb-->
            <div class="page-breadcrumb d-none d-sm-flex align-items-center mb-3">
                <div class="breadcrumb-title pe-3">
                    <a href="{{ route('cajasdux.index') }}" class="text-dark">{{ lang.cajasdux }}</a>
                </div>
                <div class="ps-3">
                    <nav aria-label="breadcrumb">
                        <ol class="breadcrumb mb-0 p-0">
                            <li class="breadcrumb-item active" aria-current="page">{{ lang.cajasdux }} {{ lang.details }}</li>
                        </ol>
                    </nav>
                </div>
                <div class="ms-auto">
                    <div class="btn-group btn-group-sm">
                        <a class="btn btn-primary btn-sm" href="{{ route('cajasdux.index') }}">
                            <i class="bi bi-reply font16"></i>{{ lang.go_back }}</a>
                    </div>
                </div>
            </div>
            <!--end breadcrumb-->
            <div class="row">
                <div class="col-xl-7 mx-auto">
                    <div class="card shadow-none border">
                        <div class="card-body">
                            <ul class="list-group">
                                   
                                <li class="list-group-item d-flex justify-content-between align-items-center flex-wrap">
                                    <h6 class="mb-0">{{ lang.cajasdux_cb_barra }}</h6>
                                    <span class="text-secondary">{{cajasdux.cb_barra}}</span>
                                </li>
                    
                                <li class="list-group-item d-flex justify-content-between align-items-center flex-wrap">
                                    <h6 class="mb-0">{{ lang.cajasdux_cb_fecha }}</h6>
                                    <span class="text-secondary">{{cajasdux.cb_fecha}}</span>
                                </li>
                    
                                <li class="list-group-item d-flex justify-content-between align-items-center flex-wrap">
                                    <h6 class="mb-0">{{ lang.cajasdux_cb_cambio }}</h6>
                                    <span class="text-secondary">{{cajasdux.cb_cambio}}</span>
                                </li>
                    
                                <li class="list-group-item d-flex justify-content-between align-items-center flex-wrap">
                                    <h6 class="mb-0">{{ lang.cajasdux_cb_efectivo }}</h6>
                                    <span class="text-secondary">{{cajasdux.cb_efectivo}}</span>
                                </li>
                    
                                <li class="list-group-item d-flex justify-content-between align-items-center flex-wrap">
                                    <h6 class="mb-0">{{ lang.cajasdux_cb_tarjeta }}</h6>
                                    <span class="text-secondary">{{cajasdux.cb_tarjeta}}</span>
                                </li>
                    
                                <li class="list-group-item d-flex justify-content-between align-items-center flex-wrap">
                                    <h6 class="mb-0">{{ lang.cajasdux_cb_total }}</h6>
                                    <span class="text-secondary">{{cajasdux.cb_total}}</span>
                                </li>
                    
                                <li class="list-group-item d-flex justify-content-between align-items-center flex-wrap">
                                    <h6 class="mb-0">{{ lang.cajasdux_cb_efectivoz }}</h6>
                                    <span class="text-secondary">{{cajasdux.cb_efectivoz}}</span>
                                </li>
                    
                                <li class="list-group-item d-flex justify-content-between align-items-center flex-wrap">
                                    <h6 class="mb-0">{{ lang.cajasdux_cb_tarjetaz }}</h6>
                                    <span class="text-secondary">{{cajasdux.cb_tarjetaz}}</span>
                                </li>
                    
                                <li class="list-group-item d-flex justify-content-between align-items-center flex-wrap">
                                    <h6 class="mb-0">{{ lang.cajasdux_cb_totalz }}</h6>
                                    <span class="text-secondary">{{cajasdux.cb_totalz}}</span>
                                </li>
                    
                                <li class="list-group-item d-flex justify-content-between align-items-center flex-wrap">
                                    <h6 class="mb-0">{{ lang.cajasdux_cb_diferencia }}</h6>
                                    <span class="text-secondary">{{cajasdux.cb_diferencia}}</span>
                                </li>
                    
                                <li class="list-group-item d-flex justify-content-between align-items-center flex-wrap">
                                    <h6 class="mb-0">{{ lang.cajasdux_cb_anticipada }}</h6>
                                    <span class="text-secondary">{{cajasdux.cb_anticipada}}</span>
                                </li>
                    
                                <li class="list-group-item d-flex justify-content-between align-items-center flex-wrap">
                                    <h6 class="mb-0">{{ lang.cajasdux_cb_camareros }}</h6>
                                    <span class="text-secondary">{{cajasdux.cb_camareros}}</span>
                                </li>
                
                               
                            </ul>
                            
                        </div>
                    <p class="text-center">
                        <a href="{{route('cajasdux.edit',{'id':cajasdux.id})}}" class="btn btn-outline-primary">{{ lang.edit }} {{ lang.cajasdux }}</a>
                    </p>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
